namespace LibgenDesktop.ViewModels.SetupSteps
{
    internal enum SetupWizardStep
    {
        FIRST_STEP = LANGUAGE,
        LANGUAGE = 1,
        SETUP_MODE,
        DATABASE_OPERATION,
        STEP_LIST,
        DOWNLOAD_MODE,
        DOWNLOAD_DUMP_INFO,
        COLLECTIONS,
        DOWNLOAD_DUMPS,
        DOWNLOAD_DUMP_LINKS,
        CREATE_DATABASE,
        IMPORT_DUMPS,
        CONFIRMATION
    }
}
